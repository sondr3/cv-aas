name: pipeline

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*.*.*"

jobs:
  pipeline:
    name: pipeline
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c
        with:
          install: true
          cache: true
          experimental: true

      - id: pnpm-cache-path
        name: pnpm cache path
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - id: pnpm-cache
        name: restore pnpm cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ${{ steps.pnpm-cache-path.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: mise //...:format

      - name: Check if things are dirty
        run: |-
          if [ -n "$(git status --porcelain)" ]; then
          	echo "::error::Workspace is dirty:"
          		git status --porcelain
          		git diff
          		exit 1
          fi

      - name: Build
        run: mise //...:build
        env:
          MY_EMAIL: ${{ secrets.MY_EMAIL }}
          MY_PHONE: ${{ secrets.MY_PHONE }}

      - name: Set up SSH
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "$DEPLOY_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "$KNOWN_HOSTS" >> ~/.ssh/known_hosts
        env:
          DEPLOY_KEY: ${{ secrets.REMOTE_KEY }}
          KNOWN_HOSTS: ${{ secrets.KNOWN_HOSTS }}

      - name: Sync to server
        if: startsWith(github.ref, 'refs/tags/v')
        run: rsync -rlptcv --delete --chmod=D2755,F644 dist/ $USER@$HOST:cv.eons.io/
        env:
          USER: ${{ secrets.REMOTE_USER }}
          HOST: ${{ secrets.REMOTE_HOST }}

      - name: Create GitHub release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: |
            publish/norwegian.pdf
            public/english.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
